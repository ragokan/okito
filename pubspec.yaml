name: okito
description: Your best coding friend. All in one; state management, navigation
  management(with dynamic routing), local storage, dependency injection,
  localization, cool extensions with best usages and with the support of best
  utilities!
version: 1.0.9
homepage: https://github.com/ragokan/okito
repository: https://github.com/ragokan/okito

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.11.0
